import { darkThemeSchema } from "./dark-tokens.js";

/**
 * list additional theme options.
 * The default theme is applied automatically and should not be listed here.
 */
export const themeOptions = {
  dark: darkThemeSchema,
  light: {},
};